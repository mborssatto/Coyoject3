import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    user: {
      // TO DO: make this dynamic (check github repo for auth)
    }
  },
  mutations: {},
  actions: {},
})
