extends layout

block content

  //- p #{result.map(r => r.name).join(", ")}

  each item in result
    h1 
      a(href='/api/events/' + item.id) #{item.name} 
    h2 #{item.artists.join(", ")}
    p #{item.date}
    p #{item.city}

  
